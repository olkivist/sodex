{% extends 'layouts/app.twig' %}

{% block content %}

	{% for category in categories %}
	
		<div class="category">
	
			<h3><a href="{{ path_for('courses.show', { category: category}) }}">{{ category }}</a></h3>
			
		</div>
	
	{% endfor %}

{% endblock %}
